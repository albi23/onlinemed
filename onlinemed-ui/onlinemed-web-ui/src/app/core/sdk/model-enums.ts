
export enum NotificationType {
  EMAIL,
  VISIT_ACCEPT,
  VISIT_REJECT,
  VISIT_CANCEL,
  VISIT_REQUEST
}

export enum RoleType {
  USER,
  MODERATOR,
  DOCTOR,
  ADMINISTRATOR
}

export enum TranslationStatus {
  valid,
  invalid
}

export enum VisitState {
  PENDING,
  ACCEPTED,
  REJECTED
}

