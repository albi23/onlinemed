package com.onlinemed.model.enums;

/**
 * Enumerated type to define all types of notifications
 */
public enum NotificationType {
    EMAIL,
    VISIT_ACCEPT,
    VISIT_REJECT,
    VISIT_CANCEL,
    VISIT_REQUEST,
}
